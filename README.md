# 📘 ArchiveSystem - Документация

## 🎯 О проекте
WPF приложение для учета архивных материалов с использованием **Serilog** для логирования всех действий.

## 🚀 Быстрый старт

### Требования
- .NET 8.0 SDK или выше
- Visual Studio 2022 или VS Code

### Установка
```bash
# Клонирование репозитория
git clone https://github.com/Teralyglack/serilog.git
cd serilog

# Восстановление зависимостей
dotnet restore

# Запуск приложения
dotnet run
```

## 📁 Структура проекта
```
ArchiveSystem/
├── Services/           # Бизнес-логика
│   └── ArchiveService.cs
├── Models/            # Модели данных
│   └── ArchiveItem.cs
├── Helpers/           # Вспомогательные классы
│   └── StringListConverter.cs
├── Views/             # Окна приложения
├── ArchiveSystem.csproj
├── App.xaml           # Точка входа
└── MainWindow.xaml    # Главное окно
```

## 📊 Функциональность

### Основные возможности
- ✅ Добавление архивных элементов
- ✅ Просмотр списка материалов
- ✅ Поиск по названию и категориям
- ✅ Логирование всех действий

### Модель данных (ArchiveItem)
```csharp
public class ArchiveItem
{
    public int Id { get; set; }
    public string Title { get; set; }
    public string Description { get; set; }
    public string Category { get; set; }
    public DateTime DateAdded { get; set; }
    public List<string> Tags { get; set; }
}
```

## 📝 Логирование с Serilog

### Настройка логов
```csharp
Log.Logger = new LoggerConfiguration()
    .MinimumLevel.Debug()
    .WriteTo.Console()
    .WriteTo.File("logs/archive-.log", rollingInterval: RollingInterval.Day)
    .CreateLogger();
```

### Примеры использования
```csharp
// Информационное сообщение
_logger.Information("Добавлен элемент: {Title}", item.Title);

// Предупреждение
_logger.Warning("Попытка добавить элемент без названия");

// Ошибка
_logger.Error(ex, "Ошибка при сохранении элемента");
```

### Уровни логирования
- `Verbose` - Подробная отладка
- `Debug` - Отладочная информация
- `Information` - Основные события
- `Warning` - Предупреждения
- `Error` - Ошибки
- `Fatal` - Критические ошибки

## 🔧 Разработка

### Добавление нового функционала
1. Создайте модель в папке `Models/`
2. Добавьте сервис в `Services/`
3. Создайте ViewModel (при использовании MVVM)
4. Добавьте View в `Views/`

### Тестирование
```bash
# Сборка проекта
dotnet build

# Запуск тестов (если есть)
dotnet test
```

## ⚙️ Конфигурация

### Файл проекта (ArchiveSystem.csproj)
```xml
<TargetFramework>net8.0-windows</TargetFramework>
<UseWPF>true</UseWPF>
```

### Зависимости
- **Serilog** - структурированное логирование
- **CommunityToolkit.Mvvm** - MVVM паттерн
- **Microsoft.Xaml.Behaviors** - поведения WPF

## 📈 Архитектура

### Слои приложения
```
Presentation Layer (WPF Views)
       ↓
Business Logic Layer (Services)
       ↓
Data Layer (Models/Entities)
```

### Паттерны
- **MVVM** для разделения логики и интерфейса
- **Repository** для доступа к данным
- **Dependency Injection** для управления зависимостями

## 🐛 Отладка

### Просмотр логов
- **Консоль** - вывод во время выполнения
- **Файлы** - `logs/archive-YYYY-MM-DD.log`
- **Уровень** - настраивается в `App.xaml.cs`

### Типичные проблемы
1. **Не запускается** - проверьте `dotnet --version`
2. **Нет логов** - проверьте права на запись в папку `logs/`
3. **Ошибки сборки** - выполните `dotnet clean && dotnet restore`

## 🔄 Работа с Git

### Команды
```bash
# Обновление репозитория
git pull origin main

# Добавление изменений
git add .
git commit -m "Описание изменений"
git push origin main
```

### .gitignore
Автоматически исключает:
- `bin/`, `obj/` - файлы сборки
- `.vs/` - настройки IDE
- `logs/` - файлы логов
- `*.user` - пользовательские настройки

## 📞 Поддержка

### Полезные ссылки
- [Serilog документация](https://docs.google.com/document/d/1N8oQm4ZU_ZxR9Zqy2VZCDQNM_Yip9sYIFdqIUMrRgAE/edit?usp=sharing)
- [GitHub репозиторий](https://github.com/Teralyglack/serilog)



**Версия:** 1.0.0  
**Последнее обновление:** 09.02.2026  
**Лицензия:** MIT
